import React from 'react';

// Mobile Home screen removed — website uses `Dashboard` and related components.
// This stub keeps imports stable if the file is referenced elsewhere.

export default function HomeScreen() {
  return (
    <div style={{ padding: 24 }}>
      <h2>Mobile Home Screen removed</h2>
      <p>This app no longer includes the mobile UI. Use the website Dashboard instead.</p>
    </div>
  );
}
